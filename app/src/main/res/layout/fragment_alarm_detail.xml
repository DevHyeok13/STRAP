<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main" android:padding="20dp">

    <EditText
        android:id="@+id/et_alarm_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@null" android:gravity="center"
        android:hint="알람 이름"
        android:padding="10dp"
        android:text="알람 이름"
        android:textColor="@color/text_title"
        android:textColorHint="@color/text_hint"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/btn_delete"
        android:layout_width="36dp" android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless" android:clickable="true"
        android:focusable="true"
        android:padding="6dp"
        android:src="@android:drawable/ic_menu_delete"
        app:layout_constraintBottom_toBottomOf="@id/et_alarm_name"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/et_alarm_name"
        app:tint="@color/text_hint" /> <View
    android:id="@+id/divider1"
    android:layout_width="match_parent"
    android:layout_height="1dp"
    android:layout_marginTop="16dp"
    android:background="#EAEAEA" app:layout_constraintTop_toBottomOf="@id/et_alarm_name" />

    <LinearLayout
        android:id="@+id/layout_timepicker"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginVertical="16dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/divider1">

        <TimePicker
            android:id="@+id/timePicker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleX="1.2" android:scaleY="1.2"
            android:timePickerMode="spinner" />
    </LinearLayout>

    <View
        android:id="@+id/divider2"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#EAEAEA"
        app:layout_constraintTop_toBottomOf="@id/layout_timepicker" />

    <LinearLayout
        android:id="@+id/layout_days"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/divider2">

        <TextView android:id="@+id/btn_sun" style="@style/DayButtonStyle" android:text="일" android:textColor="#F44336" />
        <TextView android:id="@+id/btn_mon" style="@style/DayButtonStyle" android:text="월" />
        <TextView android:id="@+id/btn_tue" style="@style/DayButtonStyle" android:text="화" />
        <TextView android:id="@+id/btn_wed" style="@style/DayButtonStyle" android:text="수" />
        <TextView android:id="@+id/btn_thu" style="@style/DayButtonStyle" android:text="목" />
        <TextView android:id="@+id/btn_fri" style="@style/DayButtonStyle" android:text="금" />
        <TextView android:id="@+id/btn_sat" style="@style/DayButtonStyle" android:text="토" android:textColor="#2196F3" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_select_routine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="이 시간에 할 루틴을 선택해주세요"
        android:textColor="@color/text_title"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layout_days" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_routine_select"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:clipToPadding="false" app:layout_constraintBottom_toTopOf="@id/layout_bottom_buttons"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_select_routine" />

    <LinearLayout
        android:id="@+id/layout_bottom_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:orientation="horizontal"
        android:weightSum="2"
        app:layout_constraintBottom_toBottomOf="parent">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_cancel"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="@drawable/bg_btn_cancel_outline"
            android:stateListAnimator="@null"
            android:text="취소"
            android:textColor="#555555"
            android:textSize="16sp"
            android:textStyle="bold" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_save"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:background="@drawable/bg_btn_save"
            android:stateListAnimator="@null"
            android:text="저장"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>